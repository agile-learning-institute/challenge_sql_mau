[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=2.0.0"
numpy = ">=1.24.0"
faker = ">=24.0.0"

[dev-packages]

[requires]
python_version = "3.12"

[scripts]
generate-logins = "python generate_logins.py"
